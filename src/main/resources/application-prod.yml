# Production environment configuration

# Backend service URLs (production)
backend:
  registration:
    url: ${BACKEND_REGISTRATION_URL:https://wis-registration-prod.azurewebsites.net}
  subscriptions:
    url: ${BACKEND_SUBSCRIPTIONS_URL:https://wis-subscriptions-prod.azurewebsites.net}
  messages:
    url: ${BACKEND_MESSAGES_URL:https://wis-message-handler-prod.azurewebsites.net}

# API keys from Azure Key Vault
api:
  keys: ${API_KEYS}

# Redis (temporarily disabled - will add later)
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisReactiveAutoConfiguration

# CORS (production domains only - strict)
cors:
  allowed-origins: https://wordsinseason.com,https://www.wordsinseason.com

# Application Insights
azure:
  application-insights:
    instrumentation-key: ${APPINSIGHTS_INSTRUMENTATIONKEY}

# Logging (minimal in production for performance)
logging:
  level:
    com.wis.apigateway: INFO
    org.springframework.cloud.gateway: WARN
    org.springframework.web: WARN
